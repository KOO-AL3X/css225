#battle functionality
from random import randint

import Main_chacter #my main charcter 


     
def class(vampire):

    def __init__(self, name, type_stat):

        #Set the variables given to the object
        self.name = name
        self.type_stat = type_stat

        #Set the rest of the variables with random numbers
        hp = 100
        attack =25
        
def class(warewolf):
    def __init__(self, name, type_stat):

        #Set the variables given to the object
        self.name = name
        self.type_stat = type_stat

        #Set the rest of the variables with random numbers
        hp = 120
        attack =20
#these weapons would be avaible later on in the story
def weapons(shotgun):
   damage= 50

    def weapons(crossbow):
     damage=80
     #starting off weapons
        def weapons(wooden stick):
            damage = 30


